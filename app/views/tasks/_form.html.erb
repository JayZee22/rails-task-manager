<%= form_with model: task do |f| %>
  <%= render 'form_fields', f: f %>

  <% unless task.new_record? %>
    <%= f.label :completed %>
    <%= f.check_box :completed %>
  <% end %>

  <%= f.submit %>
<% end %>
